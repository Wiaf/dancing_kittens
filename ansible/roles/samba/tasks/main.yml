---

- name: install samba 
  apt: 
    name: "{{ samba_prerequirements }}"
    state: present

- name: Add smbuser
  user:
    name: smbuser
    shell: /bin/false
  
- name: set samba user passwords
  shell: "(echo {{ item['smbpasswd'] }}; echo {{ item['smbpasswd'] }}) | smbpasswd -s -a {{ item['name'] }}"
  with_items: "{{ samba_users }}"
  no_log: true
  changed_when: false

# - name: create_shares | creating shared directories
  #file:
  #  path: "{{ samba_share_name + '/' + item['name'] }}"
  #  owner: "{{ item['owner']|default(omit) }}"
  #  group: "{{ item['group']|}}"
  #  mode: "{{ item['folder_perms']|default(omit) }}"
  #  state: directory
  #  become: true
  #with_items: "{{ samba_shares }}"

#- name: Make sure mountpoints exist
#  file:
#    path: "{{ samba_mount_path }}"
#    owner: wiaf
#    group: smbuser
#    mode: '1775'
#    state: directory

#- name: mount mountpoints
#  ansible.posix.mount:
#    path: "{{ samba_mount_path }}"
#    src: "UUID={{ item }}"
#    fstype: ext4
#    opts: noexec,nodev,noatime,nodiratime
#    state: mounted
#  notify: restart smbd
#  ignore_errors: true
#  when: samba_mount_path is defined

- name: Copy new smb.conf
  template:
    src: "smb.conf.j2"
    dest: /etc/samba/smb.conf
  notify: restart smbd
  tags: samba_config

- name: copy fuck_datengrab.sh
  copy:
    src: fuck_datengrab.sh
    dest: /usr/local/bin/fuck_datengrab.sh
    owner: root
    group: root
    mode: '744'
  tags: samba_recovery
  when: datengrab
